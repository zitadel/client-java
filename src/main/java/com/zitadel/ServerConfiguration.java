// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating
// with your Zitadel environment. This SDK enables you to handle resources, settings, and
// configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

package com.zitadel;

import java.util.Collections;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import javax.annotation.Nullable;

/**
 * Represents a single server entry from the OpenAPI specification.
 *
 * <p>A server URL may contain template variables (e.g. {@code
 * "https://{env}.api.example.com/v{version}"}). Use {@link #getUrl()} to resolve the URL with
 * default variable values, or {@link #getUrl(Map)} to override specific variables.
 *
 * <p>This class is immutable and thread-safe.
 *
 * @see ServerVariable
 */
public final class ServerConfiguration {

  private final String urlTemplate;
  @Nullable private final String description;
  private final Map<String, ServerVariable> variables;

  /**
   * Create a new server configuration.
   *
   * @param urlTemplate the URL template, possibly containing {@code {variable}} placeholders
   * @param description human-readable description of this server, or {@code null}
   * @param variables map of variable names to their definitions
   */
  public ServerConfiguration(
      String urlTemplate, @Nullable String description, Map<String, ServerVariable> variables) {
    this.urlTemplate = urlTemplate;
    this.description = description;
    this.variables = Collections.unmodifiableMap(new HashMap<>(variables));
  }

  /**
   * The raw URL template before variable substitution.
   *
   * @return the URL template string
   */
  public String getUrlTemplate() {
    return urlTemplate;
  }

  /**
   * Human-readable description of this server.
   *
   * @return the description, or {@code null} if not specified
   */
  @Nullable
  public String getDescription() {
    return description;
  }

  /**
   * The server variables and their definitions.
   *
   * @return an unmodifiable map of variable names to definitions
   */
  public Map<String, ServerVariable> getVariables() {
    return variables;
  }

  /**
   * Resolve the URL template using default variable values.
   *
   * @return the fully resolved URL
   */
  public String getUrl() {
    return getUrl(Collections.emptyMap());
  }

  /**
   * Resolve the URL template, substituting the given variable overrides.
   *
   * <p>Variables not present in {@code overrides} use their default values. If a variable has an
   * enum constraint, the override value is validated against the allowed values.
   *
   * @param overrides variable name to value overrides
   * @return the fully resolved URL
   * @throws IllegalArgumentException if an override value is not in the variable's enum
   */
  public String getUrl(Map<String, String> overrides) {
    String url = urlTemplate;
    for (Map.Entry<String, ServerVariable> entry : variables.entrySet()) {
      String varName = entry.getKey();
      ServerVariable var = entry.getValue();
      String value = overrides.getOrDefault(varName, var.getDefaultValue());

      List<String> enumValues = var.getEnumValues();
      if (!enumValues.isEmpty() && !enumValues.contains(value)) {
        throw new IllegalArgumentException(
            "Invalid value '"
                + value
                + "' for server variable '"
                + varName
                + "'. Allowed values: "
                + enumValues);
      }

      url = url.replace("{" + varName + "}", value);
    }
    return url;
  }
}
