// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating
// with your Zitadel environment. This SDK enables you to handle resources, settings, and
// configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

package com.zitadel.model;

import com.fasterxml.jackson.annotation.JsonProperty;
import java.time.Duration;
import java.util.ArrayList;
import java.util.List;
import javax.annotation.Nullable;

/** ApplicationServiceOIDCConfiguration. */
public class ApplicationServiceOIDCConfiguration {

  /**
   * RedirectURIs are the allowed callback URIs for the OAuth2 / OIDC flows, where the authorization
   * code or tokens will be sent to. The redirect_uri parameter in the authorization request must
   * exactly match one of these URIs.
   */
  @JsonProperty("redirectUris")
  @Nullable
  public List<String> redirectUris = new ArrayList<>();

  /**
   * ResponseTypes define whether a code, id_token token or just id_token will be returned. The
   * response_type parameter in the authorization request must exactly match one of these values.
   */
  @JsonProperty("responseTypes")
  @Nullable
  public List<ApplicationServiceOIDCResponseType> responseTypes = new ArrayList<>();

  /**
   * GrantTypes define the flow type the application is allowed to use. The grant_type parameter in
   * the token request must exactly match one of these values.
   */
  @JsonProperty("grantTypes")
  @Nullable
  public List<ApplicationServiceOIDCGrantType> grantTypes = new ArrayList<>();

  /** The {@code applicationType} property. */
  @JsonProperty("applicationType")
  @Nullable
  public ApplicationServiceOIDCApplicationType applicationType;

  /**
   * The unique OAuth2/OIDC client_id used for authentication of the application, e.g. at the token
   * endpoint.
   */
  @JsonProperty("clientId")
  @Nullable
  public String clientId;

  /** The {@code authMethodType} property. */
  @JsonProperty("authMethodType")
  @Nullable
  public ApplicationServiceOIDCAuthMethodType authMethodType;

  /**
   * PostLogoutRedirectURIs are the allowed URIs to redirect to after a logout. The
   * post_logout_redirect_uri parameter in the logout request must exactly match one of these URIs.
   */
  @JsonProperty("postLogoutRedirectUris")
  @Nullable
  public List<String> postLogoutRedirectUris = new ArrayList<>();

  /** The {@code version} property. */
  @JsonProperty("version")
  @Nullable
  public ApplicationServiceOIDCVersion version;

  /**
   * NonCompliant specifies whether the config is OIDC compliant. A production configuration SHOULD
   * be compliant. Non-compliant configurations can run into interoperability issues with OIDC
   * libraries and tools. Compliance problems are listed in the compliance_problems field.
   */
  @JsonProperty("nonCompliant")
  @Nullable
  public Boolean nonCompliant;

  /**
   * ComplianceProblems lists the problems for non-compliant configurations. In case of a compliant
   * configuration, this list is empty.
   */
  @JsonProperty("complianceProblems")
  @Nullable
  public List<ApplicationServiceOIDCLocalizedMessage> complianceProblems = new ArrayList<>();

  /**
   * DevelopmentMode can be enabled for development purposes. This allows the use of OIDC
   * non-compliant and potentially insecure settings, such as the use of HTTP redirect URIs or
   * wildcard redirect URIs.
   */
  @JsonProperty("developmentMode")
  @Nullable
  public Boolean developmentMode;

  /** The {@code accessTokenType} property. */
  @JsonProperty("accessTokenType")
  @Nullable
  public ApplicationServiceOIDCTokenType accessTokenType;

  /**
   * If AccessTokenRoleAssertion is enabled, the roles of the user are added to the access token.
   * Ensure that the access token is a JWT token and not a bearer token. And either request the
   * roles by scope or enable the user role assertion on the project.
   */
  @JsonProperty("accessTokenRoleAssertion")
  @Nullable
  public Boolean accessTokenRoleAssertion;

  /**
   * If IDTokenRoleAssertion is enabled, the roles of the user are added to the id token. Ensure
   * that either the roles are requested by scope or enable the user role assertion on the project.
   */
  @JsonProperty("idTokenRoleAssertion")
  @Nullable
  public Boolean idTokenRoleAssertion;

  /**
   * If IDTokenUserinfoAssertion is enabled, the claims of profile, email, address and phone scopes
   * are added to the id token even if an access token is issued. This can be required by some
   * applications that do not call the userinfo endpoint after authentication or directly use the
   * id_token for retrieving user information. Attention: this violates the OIDC specification,
   * which states that these claims must only be requested from the userinfo endpoint if an access
   * token is issued. This is to prevent leaking of personal information in the id token, which is
   * often stored in the browser and therefore more vulnerable.
   */
  @JsonProperty("idTokenUserinfoAssertion")
  @Nullable
  public Boolean idTokenUserinfoAssertion;

  /**
   * A Duration represents a signed, fixed-length span of time represented as a count of seconds and
   * fractions of seconds at nanosecond resolution. It is independent of any calendar and concepts
   * like "day" or "month". It is related to Timestamp in that the difference between two Timestamp
   * values is a Duration and it can be added or subtracted from a Timestamp. Range is approximately
   * +-10,000 years. # Examples Example 1: Compute Duration from two Timestamps in pseudo code.
   * Timestamp start = ...; Timestamp end = ...; Duration duration = ...; duration.seconds =
   * end.seconds - start.seconds; duration.nanos = end.nanos - start.nanos; if (duration.seconds
   * {@literal <} 0 && duration.nanos {@literal >} 0) { duration.seconds += 1; duration.nanos -=
   * 1000000000; } else if (duration.seconds {@literal >} 0 && duration.nanos {@literal <} 0) {
   * duration.seconds -= 1; duration.nanos += 1000000000; } Example 2: Compute Timestamp from
   * Timestamp + Duration in pseudo code. Timestamp start = ...; Duration duration = ...; Timestamp
   * end = ...; end.seconds = start.seconds + duration.seconds; end.nanos = start.nanos +
   * duration.nanos; if (end.nanos {@literal <} 0) { end.seconds -= 1; end.nanos += 1000000000; }
   * else if (end.nanos {@literal >}= 1000000000) { end.seconds += 1; end.nanos -= 1000000000; }
   * Example 3: Compute Duration from datetime.timedelta in Python. td = datetime.timedelta(days=3,
   * minutes=10) duration = Duration() duration.FromTimedelta(td) # JSON Mapping In JSON format, the
   * Duration type is encoded as a string rather than an object, where the string ends in the suffix
   * "s" (indicating seconds) and is preceded by the number of seconds, with nanoseconds expressed
   * as fractional seconds. For example, 3 seconds with 0 nanoseconds should be encoded in JSON
   * format as "3s", while 3 seconds and 1 nanosecond should be expressed in JSON format as
   * "3.000000001s", and 3 seconds and 1 microsecond should be expressed in JSON format as
   * "3.000001s".
   */
  @JsonProperty("clockSkew")
  @Nullable
  public Duration clockSkew;

  /**
   * AdditionalOrigins are HTTP origins (scheme + host + port) from where the API can be used
   * additional to the redirect_uris. This is useful if the application is used from an origin
   * different to the redirect_uris, e.g. if the application is a SPA served in a native app, where
   * the redirect_uri is a custom scheme, but the application is served from a https origin.
   */
  @JsonProperty("additionalOrigins")
  @Nullable
  public List<String> additionalOrigins = new ArrayList<>();

  /**
   * AllowedOrigins are all HTTP origins where the application is allowed to be used from. This is
   * used to prevent CORS issues in browsers. If the origin of the request is not in this list, the
   * request will be rejected. This is especially important for SPAs. Note that this is a generated
   * list from the redirect_uris and additional_origins. If you use the application from another
   * origin, you have to add it to the additional_origins.
   */
  @JsonProperty("allowedOrigins")
  @Nullable
  public List<String> allowedOrigins = new ArrayList<>();

  /**
   * For native apps a successful login usually shows a success page with a link to open the
   * application again. SkipNativeAppSuccessPage can be used to skip this page and open the
   * application directly.
   */
  @JsonProperty("skipNativeAppSuccessPage")
  @Nullable
  public Boolean skipNativeAppSuccessPage;

  /**
   * BackChannelLogoutURI is used to notify the application about terminated sessions according to
   * the OIDC Back-Channel Logout (https://openid.net/specs/openid-connect-backchannel-1_0.html).
   */
  @JsonProperty("backChannelLogoutUri")
  @Nullable
  public String backChannelLogoutUri;

  /** The {@code loginVersion} property. */
  @JsonProperty("loginVersion")
  @Nullable
  public ApplicationServiceLoginVersion loginVersion;

  /** Creates an empty instance. */
  public ApplicationServiceOIDCConfiguration() {}

  /* Value-equality based on all declared fields. Generated so model
   * instances work correctly as Set/Map keys and in test assertions.
   * Uses pattern-matching `instanceof other` (Java 16+) — required by
   * Error Prone's PatternMatchingInstanceof rule and avoids the
   * EqualsGetClass warning that plain getClass() comparison triggers. */
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (!(o instanceof ApplicationServiceOIDCConfiguration other)) {
      return false;
    }
    return com.zitadel.ObjectSerializer.structuralEquals(this.redirectUris, other.redirectUris)
        && com.zitadel.ObjectSerializer.structuralEquals(this.responseTypes, other.responseTypes)
        && com.zitadel.ObjectSerializer.structuralEquals(this.grantTypes, other.grantTypes)
        && java.util.Objects.equals(this.applicationType, other.applicationType)
        && java.util.Objects.equals(this.clientId, other.clientId)
        && java.util.Objects.equals(this.authMethodType, other.authMethodType)
        && com.zitadel.ObjectSerializer.structuralEquals(
            this.postLogoutRedirectUris, other.postLogoutRedirectUris)
        && java.util.Objects.equals(this.version, other.version)
        && java.util.Objects.equals(this.nonCompliant, other.nonCompliant)
        && com.zitadel.ObjectSerializer.structuralEquals(
            this.complianceProblems, other.complianceProblems)
        && java.util.Objects.equals(this.developmentMode, other.developmentMode)
        && java.util.Objects.equals(this.accessTokenType, other.accessTokenType)
        && java.util.Objects.equals(this.accessTokenRoleAssertion, other.accessTokenRoleAssertion)
        && java.util.Objects.equals(this.idTokenRoleAssertion, other.idTokenRoleAssertion)
        && java.util.Objects.equals(this.idTokenUserinfoAssertion, other.idTokenUserinfoAssertion)
        && java.util.Objects.equals(this.clockSkew, other.clockSkew)
        && com.zitadel.ObjectSerializer.structuralEquals(
            this.additionalOrigins, other.additionalOrigins)
        && com.zitadel.ObjectSerializer.structuralEquals(this.allowedOrigins, other.allowedOrigins)
        && java.util.Objects.equals(this.skipNativeAppSuccessPage, other.skipNativeAppSuccessPage)
        && java.util.Objects.equals(this.backChannelLogoutUri, other.backChannelLogoutUri)
        && java.util.Objects.equals(this.loginVersion, other.loginVersion);
  }

  @Override
  public int hashCode() {
    return java.util.Objects.hash(
        com.zitadel.ObjectSerializer.structuralHashCode(redirectUris),
        com.zitadel.ObjectSerializer.structuralHashCode(responseTypes),
        com.zitadel.ObjectSerializer.structuralHashCode(grantTypes),
        applicationType,
        clientId,
        authMethodType,
        com.zitadel.ObjectSerializer.structuralHashCode(postLogoutRedirectUris),
        version,
        nonCompliant,
        com.zitadel.ObjectSerializer.structuralHashCode(complianceProblems),
        developmentMode,
        accessTokenType,
        accessTokenRoleAssertion,
        idTokenRoleAssertion,
        idTokenUserinfoAssertion,
        clockSkew,
        com.zitadel.ObjectSerializer.structuralHashCode(additionalOrigins),
        com.zitadel.ObjectSerializer.structuralHashCode(allowedOrigins),
        skipNativeAppSuccessPage,
        backChannelLogoutUri,
        loginVersion);
  }
}
