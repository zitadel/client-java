/*
 * Zitadel SDK
 * The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.zitadel.model;

import java.util.Objects;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import com.zitadel.model.UserServiceAuthFactorState;
import com.zitadel.model.UserServiceAuthFactors;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.fasterxml.jackson.annotation.JsonTypeName;
import java.io.UnsupportedEncodingException;
import java.net.URLEncoder;
import java.util.StringJoiner;

/**
 * UserServiceListAuthenticationFactorsRequest
 */
@JsonPropertyOrder({
  UserServiceListAuthenticationFactorsRequest.JSON_PROPERTY_USER_ID,
  UserServiceListAuthenticationFactorsRequest.JSON_PROPERTY_AUTH_FACTORS,
  UserServiceListAuthenticationFactorsRequest.JSON_PROPERTY_STATES
})
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.13.0")
public class UserServiceListAuthenticationFactorsRequest {
  public static final String JSON_PROPERTY_USER_ID = "userId";
  @javax.annotation.Nonnull
  private String userId;

  public static final String JSON_PROPERTY_AUTH_FACTORS = "authFactors";
  @javax.annotation.Nullable
  private List<UserServiceAuthFactors> authFactors = new ArrayList<>();

  public static final String JSON_PROPERTY_STATES = "states";
  @javax.annotation.Nullable
  private List<UserServiceAuthFactorState> states = new ArrayList<>();

  public UserServiceListAuthenticationFactorsRequest() {
  }

  public UserServiceListAuthenticationFactorsRequest userId(@javax.annotation.Nonnull String userId) {
    
    this.userId = userId;
    return this;
  }

  /**
   * Get userId
   * @return userId
   */
  @javax.annotation.Nonnull
  @JsonProperty(JSON_PROPERTY_USER_ID)
  @JsonInclude(value = JsonInclude.Include.ALWAYS)

  public String getUserId() {
    return userId;
  }


  @JsonProperty(JSON_PROPERTY_USER_ID)
  @JsonInclude(value = JsonInclude.Include.ALWAYS)
  public void setUserId(@javax.annotation.Nonnull String userId) {
    this.userId = userId;
  }

  public UserServiceListAuthenticationFactorsRequest authFactors(@javax.annotation.Nullable List<UserServiceAuthFactors> authFactors) {
    
    this.authFactors = authFactors;
    return this;
  }

  public UserServiceListAuthenticationFactorsRequest addAuthFactorsItem(UserServiceAuthFactors authFactorsItem) {
    if (this.authFactors == null) {
      this.authFactors = new ArrayList<>();
    }
    this.authFactors.add(authFactorsItem);
    return this;
  }

  /**
   * Get authFactors
   * @return authFactors
   */
  @javax.annotation.Nullable
  @JsonProperty(JSON_PROPERTY_AUTH_FACTORS)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)

  public List<UserServiceAuthFactors> getAuthFactors() {
    return authFactors;
  }


  @JsonProperty(JSON_PROPERTY_AUTH_FACTORS)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
  public void setAuthFactors(@javax.annotation.Nullable List<UserServiceAuthFactors> authFactors) {
    this.authFactors = authFactors;
  }

  public UserServiceListAuthenticationFactorsRequest states(@javax.annotation.Nullable List<UserServiceAuthFactorState> states) {
    
    this.states = states;
    return this;
  }

  public UserServiceListAuthenticationFactorsRequest addStatesItem(UserServiceAuthFactorState statesItem) {
    if (this.states == null) {
      this.states = new ArrayList<>();
    }
    this.states.add(statesItem);
    return this;
  }

  /**
   * Get states
   * @return states
   */
  @javax.annotation.Nullable
  @JsonProperty(JSON_PROPERTY_STATES)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)

  public List<UserServiceAuthFactorState> getStates() {
    return states;
  }


  @JsonProperty(JSON_PROPERTY_STATES)
  @JsonInclude(value = JsonInclude.Include.USE_DEFAULTS)
  public void setStates(@javax.annotation.Nullable List<UserServiceAuthFactorState> states) {
    this.states = states;
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    UserServiceListAuthenticationFactorsRequest userServiceListAuthenticationFactorsRequest = (UserServiceListAuthenticationFactorsRequest) o;
    return Objects.equals(this.userId, userServiceListAuthenticationFactorsRequest.userId) &&
        Objects.equals(this.authFactors, userServiceListAuthenticationFactorsRequest.authFactors) &&
        Objects.equals(this.states, userServiceListAuthenticationFactorsRequest.states);
  }

  @Override
  public int hashCode() {
    return Objects.hash(userId, authFactors, states);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class UserServiceListAuthenticationFactorsRequest {\n");
    sb.append("    userId: ").append(toIndentedString(userId)).append("\n");
    sb.append("    authFactors: ").append(toIndentedString(authFactors)).append("\n");
    sb.append("    states: ").append(toIndentedString(states)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }

  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    // add `userId` to the URL query string
    if (getUserId() != null) {
      try {
        joiner.add(String.format("%suserId%s=%s", prefix, suffix, URLEncoder.encode(String.valueOf(getUserId()), "UTF-8").replaceAll("\\+", "%20")));
      } catch (UnsupportedEncodingException e) {
        // Should never happen, UTF-8 is always supported
        throw new RuntimeException(e);
      }
    }

    // add `authFactors` to the URL query string
    if (getAuthFactors() != null) {
      for (int i = 0; i < getAuthFactors().size(); i++) {
        if (getAuthFactors().get(i) != null) {
          try {
            joiner.add(String.format("%sauthFactors%s%s=%s", prefix, suffix,
                "".equals(suffix) ? "" : String.format("%s%d%s", containerPrefix, i, containerSuffix),
                URLEncoder.encode(String.valueOf(getAuthFactors().get(i)), "UTF-8").replaceAll("\\+", "%20")));
          } catch (UnsupportedEncodingException e) {
            // Should never happen, UTF-8 is always supported
            throw new RuntimeException(e);
          }
        }
      }
    }

    // add `states` to the URL query string
    if (getStates() != null) {
      for (int i = 0; i < getStates().size(); i++) {
        if (getStates().get(i) != null) {
          try {
            joiner.add(String.format("%sstates%s%s=%s", prefix, suffix,
                "".equals(suffix) ? "" : String.format("%s%d%s", containerPrefix, i, containerSuffix),
                URLEncoder.encode(String.valueOf(getStates().get(i)), "UTF-8").replaceAll("\\+", "%20")));
          } catch (UnsupportedEncodingException e) {
            // Should never happen, UTF-8 is always supported
            throw new RuntimeException(e);
          }
        }
      }
    }

    return joiner.toString();
  }

}

