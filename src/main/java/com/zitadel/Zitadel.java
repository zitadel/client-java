// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating
// with your Zitadel environment. This SDK enables you to handle resources, settings, and
// configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

package com.zitadel;

import com.zitadel.api.ActionServiceApi;
import com.zitadel.api.ApplicationServiceApi;
import com.zitadel.api.AuthorizationServiceApi;
import com.zitadel.api.BetaActionServiceApi;
import com.zitadel.api.BetaAppServiceApi;
import com.zitadel.api.BetaAuthorizationServiceApi;
import com.zitadel.api.BetaFeatureServiceApi;
import com.zitadel.api.BetaInstanceServiceApi;
import com.zitadel.api.BetaInternalPermissionServiceApi;
import com.zitadel.api.BetaOIDCServiceApi;
import com.zitadel.api.BetaOrganizationServiceApi;
import com.zitadel.api.BetaProjectServiceApi;
import com.zitadel.api.BetaSessionServiceApi;
import com.zitadel.api.BetaSettingsServiceApi;
import com.zitadel.api.BetaTelemetryServiceApi;
import com.zitadel.api.BetaUserServiceApi;
import com.zitadel.api.BetaWebKeyServiceApi;
import com.zitadel.api.FeatureServiceApi;
import com.zitadel.api.IdentityProviderServiceApi;
import com.zitadel.api.InstanceServiceApi;
import com.zitadel.api.InternalPermissionServiceApi;
import com.zitadel.api.OIDCServiceApi;
import com.zitadel.api.OrganizationServiceApi;
import com.zitadel.api.ProjectServiceApi;
import com.zitadel.api.SAMLServiceApi;
import com.zitadel.api.SessionServiceApi;
import com.zitadel.api.SettingsServiceApi;
import com.zitadel.api.UserServiceApi;
import com.zitadel.api.WebKeyServiceApi;
import com.zitadel.auth.Authenticator;
import com.zitadel.auth.BearerAuthenticator;
import com.zitadel.auth.HttpAwareAuthenticator;
import edu.umd.cs.findbugs.annotations.SuppressFBWarnings;

/**
 * Unified entry point for all API services.
 *
 * <p>Takes an {@link Authenticator} and optionally {@link TransportOptions}, then exposes each API
 * group as a typed property. If the authenticator implements {@link HttpAwareAuthenticator}, the
 * shared {@link ApiClient} is injected so that authentication HTTP calls (token exchange,
 * discovery) use the same transport configuration as regular API calls.
 *
 * <p>Usage:
 *
 * <pre>{@code
 * // Default transport
 * Zitadel client = new Zitadel(authenticator);
 *
 * // Custom transport (proxy, timeouts, etc.)
 * TransportOptions transport = TransportOptions.builder()
 *     .proxy("http://proxy:3128")
 *     .timeout(5000)
 *     .build();
 * Zitadel client = new Zitadel(authenticator, transport);
 * }</pre>
 */
@SuppressFBWarnings(
    value = {"URF_UNREAD_PUBLIC_OR_PROTECTED_FIELD"},
    justification = "generated code")
public final class Zitadel {

  /** API operations for the ActionServiceApi group. */
  public final ActionServiceApi actionService;

  /** API operations for the ApplicationServiceApi group. */
  public final ApplicationServiceApi applicationService;

  /** API operations for the AuthorizationServiceApi group. */
  public final AuthorizationServiceApi authorizationService;

  /** API operations for the BetaActionServiceApi group. */
  public final BetaActionServiceApi betaActionService;

  /** API operations for the BetaAppServiceApi group. */
  public final BetaAppServiceApi betaAppService;

  /** API operations for the BetaAuthorizationServiceApi group. */
  public final BetaAuthorizationServiceApi betaAuthorizationService;

  /** API operations for the BetaFeatureServiceApi group. */
  public final BetaFeatureServiceApi betaFeatureService;

  /** API operations for the BetaInstanceServiceApi group. */
  public final BetaInstanceServiceApi betaInstanceService;

  /** API operations for the BetaInternalPermissionServiceApi group. */
  public final BetaInternalPermissionServiceApi betaInternalPermissionService;

  /** API operations for the BetaOIDCServiceApi group. */
  public final BetaOIDCServiceApi betaOIDCService;

  /** API operations for the BetaOrganizationServiceApi group. */
  public final BetaOrganizationServiceApi betaOrganizationService;

  /** API operations for the BetaProjectServiceApi group. */
  public final BetaProjectServiceApi betaProjectService;

  /** API operations for the BetaSessionServiceApi group. */
  public final BetaSessionServiceApi betaSessionService;

  /** API operations for the BetaSettingsServiceApi group. */
  public final BetaSettingsServiceApi betaSettingsService;

  /** API operations for the BetaTelemetryServiceApi group. */
  public final BetaTelemetryServiceApi betaTelemetryService;

  /** API operations for the BetaUserServiceApi group. */
  public final BetaUserServiceApi betaUserService;

  /** API operations for the BetaWebKeyServiceApi group. */
  public final BetaWebKeyServiceApi betaWebKeyService;

  /** API operations for the FeatureServiceApi group. */
  public final FeatureServiceApi featureService;

  /** API operations for the IdentityProviderServiceApi group. */
  public final IdentityProviderServiceApi identityProviderService;

  /** API operations for the InstanceServiceApi group. */
  public final InstanceServiceApi instanceService;

  /** API operations for the InternalPermissionServiceApi group. */
  public final InternalPermissionServiceApi internalPermissionService;

  /** API operations for the OIDCServiceApi group. */
  public final OIDCServiceApi oIDCService;

  /** API operations for the OrganizationServiceApi group. */
  public final OrganizationServiceApi organizationService;

  /** API operations for the ProjectServiceApi group. */
  public final ProjectServiceApi projectService;

  /** API operations for the SAMLServiceApi group. */
  public final SAMLServiceApi sAMLService;

  /** API operations for the SessionServiceApi group. */
  public final SessionServiceApi sessionService;

  /** API operations for the SettingsServiceApi group. */
  public final SettingsServiceApi settingsService;

  /** API operations for the UserServiceApi group. */
  public final UserServiceApi userService;

  /** API operations for the WebKeyServiceApi group. */
  public final WebKeyServiceApi webKeyService;

  /**
   * Creates a new client with the given authenticator and default transport settings.
   *
   * @param authenticator provides host URL and auth credentials
   * @throws ApiException if the transport cannot be configured (never thrown for default transport,
   *     which configures no custom CA certificate)
   */
  public Zitadel(Authenticator authenticator) {
    this(authenticator, TransportOptions.builder().build());
  }

  /**
   * Creates a new client with the given authenticator and transport options.
   *
   * <p>If the authenticator implements {@link HttpAwareAuthenticator}, the shared {@link ApiClient}
   * is injected so that token exchange and discovery requests use the same proxy, TLS, and timeout
   * settings.
   *
   * @param authenticator provides host URL and auth credentials
   * @param transportOptions HTTP transport configuration (proxy, TLS, timeouts, etc.)
   * @throws ApiException if a configured custom CA certificate cannot be read or parsed
   */
  public Zitadel(Authenticator authenticator, TransportOptions transportOptions) {
    ApiClient apiClient = new DefaultApiClient(transportOptions);

    if (authenticator instanceof HttpAwareAuthenticator httpAware) {
      httpAware.setApiClient(apiClient);
    }

    Configuration config = Configuration.builder().baseUrl(authenticator.getHost()).build();
    this.actionService = new ActionServiceApi(apiClient, config, authenticator);
    this.applicationService = new ApplicationServiceApi(apiClient, config, authenticator);
    this.authorizationService = new AuthorizationServiceApi(apiClient, config, authenticator);
    this.betaActionService = new BetaActionServiceApi(apiClient, config, authenticator);
    this.betaAppService = new BetaAppServiceApi(apiClient, config, authenticator);
    this.betaAuthorizationService =
        new BetaAuthorizationServiceApi(apiClient, config, authenticator);
    this.betaFeatureService = new BetaFeatureServiceApi(apiClient, config, authenticator);
    this.betaInstanceService = new BetaInstanceServiceApi(apiClient, config, authenticator);
    this.betaInternalPermissionService =
        new BetaInternalPermissionServiceApi(apiClient, config, authenticator);
    this.betaOIDCService = new BetaOIDCServiceApi(apiClient, config, authenticator);
    this.betaOrganizationService = new BetaOrganizationServiceApi(apiClient, config, authenticator);
    this.betaProjectService = new BetaProjectServiceApi(apiClient, config, authenticator);
    this.betaSessionService = new BetaSessionServiceApi(apiClient, config, authenticator);
    this.betaSettingsService = new BetaSettingsServiceApi(apiClient, config, authenticator);
    this.betaTelemetryService = new BetaTelemetryServiceApi(apiClient, config, authenticator);
    this.betaUserService = new BetaUserServiceApi(apiClient, config, authenticator);
    this.betaWebKeyService = new BetaWebKeyServiceApi(apiClient, config, authenticator);
    this.featureService = new FeatureServiceApi(apiClient, config, authenticator);
    this.identityProviderService = new IdentityProviderServiceApi(apiClient, config, authenticator);
    this.instanceService = new InstanceServiceApi(apiClient, config, authenticator);
    this.internalPermissionService =
        new InternalPermissionServiceApi(apiClient, config, authenticator);
    this.oIDCService = new OIDCServiceApi(apiClient, config, authenticator);
    this.organizationService = new OrganizationServiceApi(apiClient, config, authenticator);
    this.projectService = new ProjectServiceApi(apiClient, config, authenticator);
    this.sAMLService = new SAMLServiceApi(apiClient, config, authenticator);
    this.sessionService = new SessionServiceApi(apiClient, config, authenticator);
    this.settingsService = new SettingsServiceApi(apiClient, config, authenticator);
    this.userService = new UserServiceApi(apiClient, config, authenticator);
    this.webKeyService = new WebKeyServiceApi(apiClient, config, authenticator);
  }

  /**
   * Creates a client authenticated with a static Bearer token and default transport.
   *
   * @param host API base URL
   * @param accessToken Bearer token
   * @param transportOptions optional HTTP transport configuration (proxy, TLS, timeouts, etc.)
   * @return configured client instance
   * @throws ApiException if a configured custom CA certificate cannot be read or parsed
   */
  public static Zitadel withToken(
      String host,
      String accessToken,
      @javax.annotation.Nullable TransportOptions transportOptions) {
    return (transportOptions != null)
        ? new Zitadel(new BearerAuthenticator(host, accessToken), transportOptions)
        : new Zitadel(new BearerAuthenticator(host, accessToken));
  }

  /**
   * Creates a client authenticated with a static Bearer token and default transport.
   *
   * @param host API base URL
   * @param accessToken Bearer token
   * @return configured client instance
   * @throws ApiException if a configured custom CA certificate cannot be read or parsed
   */
  public static Zitadel withToken(String host, String accessToken) {
    return withToken(host, accessToken, null);
  }

  /**
   * Creates a client from any {@link Authenticator} implementation with default transport.
   *
   * <p>This is the generic entry point for bespoke authenticators (client credentials, JWT private
   * key, personal access token, etc.): construct the authenticator and pass it here, e.g. {@code
   * Zitadel.withAuthenticator(new ClientCredentialsAuthenticator(host, id, secret))}.
   *
   * @param authenticator provides host URL and auth credentials
   * @return configured client instance
   * @throws ApiException if a configured custom CA certificate cannot be read or parsed
   */
  public static Zitadel withAuthenticator(Authenticator authenticator) {
    return new Zitadel(authenticator);
  }

  /**
   * Creates a client from any {@link Authenticator} implementation with the given transport.
   *
   * @param authenticator provides host URL and auth credentials
   * @param transportOptions HTTP transport configuration (proxy, TLS, timeouts, etc.)
   * @return configured client instance
   * @throws ApiException if a configured custom CA certificate cannot be read or parsed
   */
  public static Zitadel withAuthenticator(
      Authenticator authenticator, TransportOptions transportOptions) {
    return new Zitadel(authenticator, transportOptions);
  }
}
